app_certification:
    resource: "@AppBundle/Controller/CertificationController.php"
    type:     annotation

app_skill:
    resource: "@AppBundle/Controller/SkillController.php"
    type:     annotation

app_experience:
    resource: "@AppBundle/Controller/ExperienceController.php"
    type:     annotation

app_user:
    resource: "@AppBundle/Controller/UserController.php"
    type:     annotation

app_education:
    resource: "@AppBundle/Controller/EducationController.php"
    type:     annotation

app_user_social_networking:
    resource: "@AppBundle/Controller/UserSocialNetworkingController.php"
    type:     annotation

app_social_networking:
    resource: "@AppBundle/Controller/SocialNetworkingController.php"
    type:     annotation

app_homepage:
    path:   /{_locale}
    defaults: {_controller: AppBundle:Default:index, _locale: pt_BR}
    requirements:
        _locale: "en|pt_BR"

app_curriculum:
    path:   /{username}/curriculum/{generate}/{_locale}
    defaults: {_controller: AppBundle:Default:curriculum, _locale: pt_BR, generate: pdf}
    requirements:
        _locale: "en|pt_BR"
        generate: "html|pdf"

app_send_contact:
    path:   /{username}/send_contact/{_locale}
    defaults: {_controller: AppBundle:Default:sendContact, _locale: pt_BR}
    requirements:
        _locale: "en|pt_BR"

app_profile:
    path:   /{username}/{_locale}
    defaults: {_controller: AppBundle:Default:profile, _locale: pt_BR }
    requirements:
        _locale: "en|pt_BR"

fos_user:
    resource: "@FOSUserBundle/Resources/config/routing/all.xml"
    prefix:   /{_locale}
    defaults: {_locale: pt_BR}
    requirements:
            _locale: "en|pt_BR"