{% include 'component/_email.html.twig' with {
    'title': 'email.hello'|trans({ '%firstName%': user.firstName, '%lastName%': user.lastName }),
    'message_hidden': 'email.reset_password.message_hidden'|trans,
    'message': 'email.reset_password.message'|trans,
    'additional_info': 'email.reset_password.additional_info'|trans({ '%hour%': tokenLifetime|date('g') }),
    'button': {
        'name' : 'email.reset_password.button'|trans,
        'link': url('app_reset_password', {token: resetToken.token})
    }
} %}
