{% extends 'profile/base.html.twig' %}

{% block title %}{{title|trans}}{% endblock %}
{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {% include "profile/partial/form/submit_button.html.twig" with {
        'back_path': path(back_path),
        'id': 'submit-button'
    }%}
    {{ form_end(form) }}
{% endblock %}
