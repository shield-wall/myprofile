{% extends 'base_profile.html.twig' %}

{% block container_profile %}
    <h1>Socialnetworking creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_socialnetworking_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
